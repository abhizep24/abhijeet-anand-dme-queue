# Agent dependencies (the world server's own deps live in env/requirements.txt).
anthropic>=0.39.0
truststore>=0.9.0  # use OS keychain for TLS (corporate cert-inspection proxies)
httpx>=0.27.0
pyyaml>=6.0.2
python-dotenv>=1.0.1
